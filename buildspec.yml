version: 0.1

pahses:
  build:
    commands:
      - echo Entered build phase
      - mvn clean package
  post_build:
    commands:
      - echo Build success
artifacts:
  files:
    - target/medview-app.jar
  discard-paths: yes
